" To use OmniCppComplete uncommented bellow lines,
" this is helpful for linux kernel code completion because
" clang_complete could not process assembler code other than X86
"call remove(g:pathogen_disabled, -1)
"call add(g:pathogen_disabled, 'clang_complete')
"call add(g:pathogen_disabled, 'neocomplcache-clang_complete')
